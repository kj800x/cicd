apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: cicd
  name: cicd
  namespace: cicd
spec:
  replicas: 1
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: cicd
  template:
    metadata:
      labels:
        app: cicd
    spec:
      containers:
        - command:
            - cicd
          env:
            - name: WEBSOCKET_URL
              value: ws://apps.coolkev.com.:8080/ws
            - name: ENABLE_K8S_CONTROLLER
              value: 'true'
            - name: TEMPLATE_NAMESPACE
              value: 'template-namespace'
            - name: DATABASE_PATH
              value: /data/db.db
            - name: GITHUB_APP_ID
              valueFrom:
                secretKeyRef:
                  key: GITHUB_APP_ID
                  name: cicd-auth
            - name: GITHUB_APP_PRIVATE_KEY
              valueFrom:
                secretKeyRef:
                  key: GITHUB_APP_PRIVATE_KEY
                  name: cicd-auth
            - name: CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  key: CLIENT_SECRET
                  name: cicd-auth
            - name: DISCORD_BOT_TOKEN
              valueFrom:
                secretKeyRef:
                  key: DISCORD_BOT_TOKEN
                  name: cicd-auth
            - name: DISCORD_CHANNEL_ID
              valueFrom:
                secretKeyRef:
                  key: DISCORD_CHANNEL_ID
                  name: cicd-auth
          image: ghcr.io/kj800x/cicd:commit-$SHA
          name: cicd
          ports:
            - containerPort: 8080
              protocol: TCP
          volumeMounts:
            - mountPath: /data
              name: cicd-data
          workingDir: /usr/src/cicd
      imagePullSecrets:
        - name: ghcr-kj800x
      serviceAccountName: cicd-operator
      volumes:
        # The pod is pinned to the NAS (nodeSelector below), so the sqlite
        # database is read straight from the local ZFS dataset. Over NFS
        # from the same node every query paid lock and revalidation round
        # trips, about 2 ms each, which made every page slow.
        - name: cicd-data
          hostPath:
            path: /data/services/cicd
            type: Directory
      nodeSelector:
        dedicated: nas
